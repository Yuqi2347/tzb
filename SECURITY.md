# Security

Do not commit API keys, model provider tokens, private dataset paths, or generated files containing confidential data.

Before publishing this repository, revoke any API keys that appeared in earlier private commits or shared archives. Removing keys from the current working tree does not invalidate already exposed credentials.
